# Infrastructure / Player

## Phase A Status

Empty scaffold.

## Phase C Plan

- `MpvPlayerAdapter(PlayerPort)` — cross-platform via `python-mpv`.
- `WinRTPlayerAdapter(PlayerPort)` — Windows-native `MediaPlayer` (Windows only).
