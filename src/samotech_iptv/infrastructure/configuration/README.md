# Infrastructure / Configuration

## Phase A Status

Empty scaffold.

## Phase B Plan

- `TomlConfigLoader` — reads `~/.samotech_iptv/config.toml` and merges
  with environment variables to produce `AppConfig`.
