[pytest]
testpaths = tests
asyncio_mode = auto
addopts = --strict-markers -q
markers =
    unit: Pure unit tests with no I/O
    integration: Tests that use real SQLite / network stubs
    e2e: End-to-end application launch tests
