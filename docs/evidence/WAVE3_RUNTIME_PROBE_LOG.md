# Wave 3 Controlled Runtime Probe Log

## Scope

This log records one controlled VOD HLS probe using the existing Phase 26 real-libVLC harness. The fixture was a public Mux HLS test stream documented as a test source; it was not a commercial IPTV provider, and it does not satisfy authorized-provider acceptance.

## Probe Result

| Field | Result |
|---|---|
| Mode | Existing `phase26_real_playback_harness.py --mode real` |
| Content class | MOVIE / public HLS test fixture |
| Evidence identifier | `d6347874ccd4b656` |
| Result | **ENVIRONMENTAL BLOCKER** |
| Samples | 0 |
| Recovery attempts | 0 |
| Immediate cause | `VlcPlayerAdapter()` failed with `NameError` during native libVLC initialization |
| Host runtime check | No `libvlc.so`, `libvlccore.so`, VLC executable, or configured VLC runtime directory was present in the Linux sandbox |
| Sensitive-material result | No credential, authorization-header, or configured-stream URL was stored in the result record |

The failure occurred before a libVLC media object was opened. It is therefore invalid to classify this as an HLS manifest, demux, decoded-frame, audio-initialization, buffering, or provider defect.

## Evidence Boundaries

The Mux fixture is a public test source only. It can help distinguish a local runtime dependency failure from an account/credential problem, but it cannot prove Xtream, MAG, subscription, token, cookie, media-header, or sustained real-provider compatibility.

## Next Required Runtime Evidence

Run the same existing sanitized harness on a Windows host with the packaged VLC runtime present, first against a documented public fixture and then against a newly supplied authorized provider source. Capture only safe hashes, state transitions, relative timings, event classes, recovery counts, frame/audio observations if the instrumentation can prove them, and release no raw stream data.

## References

[1]: [Mux Test HLS Streams](https://test-streams.mux.dev/) — public test-source statement.  
[2]: `build/WAVE3_PUBLIC_HLS_VOD_EVIDENCE.json` — sanitized harness evidence generated by this probe.  
[3]: `tools/phase26_real_playback_harness.py` — existing real-mode harness and redaction contract.
